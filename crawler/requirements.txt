requests>=2.31
beautifulsoup4>=4.12
# lxml ускоряет парсинг, но на shared-хостинге часто не собирается.
# Если pip install lxml падает — можно его пропустить: краулер сам
# перейдёт на встроенный html.parser. Ставить по возможности:
lxml>=5.0
# anthropic нужен ТОЛЬКО для точного извлечения через Claude (без --no-llm).
# На сервере обычно запускают с --no-llm — тогда этот пакет не требуется.
anthropic>=0.40
